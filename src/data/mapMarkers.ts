import type { MapMarker } from '../types/genshin';

// The local layer starts empty so the app never invents world coordinates.
// Comprehensive world markers are provided by the embedded community map.
export const starterMarkers: MapMarker[] = [];
