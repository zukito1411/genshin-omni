import { ExternalLink, Info } from 'lucide-react';
import { SectionTitle } from '../components/SectionTitle';
import { sources } from '../data/sources';

export function SourcesPage() {
  return <div><SectionTitle eyebrow="DATA & ATTRIBUTION" title="Sources and data boundaries" description="The app combines structured public APIs where browser access is appropriate and links to community sites for theorycrafting that is not safely scrapeable from a client-only site." /><div className="source-grid">{sources.map((source) => <article className="panel source-card" key={source.name}><div className="eyebrow">PROVIDER</div><h3>{source.name}</h3><p>{source.purpose}</p><a className="source-button" href={source.url} target="_blank" rel="noreferrer">Open source <ExternalLink size={13} /></a></article>)}</div><section className="panel boundary-panel"><Info size={18} /><div><h3>Why not scrape every guide site directly?</h3><p>A browser app cannot reliably bypass another site's CORS rules, robots controls, anti-bot behavior or changing HTML. Teyvat Atlas therefore uses structured public APIs for live data, and gives users one-click access to major guide sites rather than pretending their pages are an API.</p><p>The GenshinDB project documents its public API as a web endpoint over its structured dataset, and genshin.dev documents static entity/image endpoints. Enka documents public UID lookup and rate limits. cite-placeholder</p></div></section><section className="callout"><div><div className="eyebrow">CLIENT ONLY</div><h3>No application server or database is required.</h3><p>Remote data is cached in browser storage. Favorites, teams, material checklists and map progress stay on the user's device.</p></div><a className="button secondary" href="https://github.com/theBowja/genshin-db" target="_blank" rel="noreferrer">Inspect data source</a></section></div>;
}
