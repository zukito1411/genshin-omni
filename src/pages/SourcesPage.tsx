import { ExternalLink, Info } from 'lucide-react';
import { SectionTitle } from '../components/SectionTitle';
import { sources } from '../data/sources';

export function SourcesPage() {
  return <div><SectionTitle eyebrow="DATA & ATTRIBUTION" title="Sources" description="Teyvat Atlas combines public structured game data, player-build references and a live interactive map." /><div className="source-grid">{sources.map((source) => <article className="panel source-card" key={source.name}><div className="eyebrow">SOURCE</div><h3>{source.name}</h3><p>{source.purpose}</p><a className="source-button" href={source.url} target="_blank" rel="noreferrer">Open source <ExternalLink size={13} /></a></article>)}</div><section className="panel boundary-panel"><Info size={18} /><div><h3>How the data works</h3><p>Character discovery comes from the live GenshinDB roster. Artwork starts with the live game asset fields and falls back through genshin.dev plus public game UI CDNs when an exact asset filename is available. Build information is requested when a player opens a character, using a public reader to make the current build page browser-readable.</p><p>Successful responses are cached locally so temporary provider failures do not immediately erase information already viewed on this device.</p></div></section><section className="callout"><div><div className="eyebrow">CLIENT ONLY</div><h3>No application server or database</h3><p>Teyvat Atlas is a static Vite application. Favorites, teams, material checklists and personal map pins remain in browser storage.</p></div><a className="button secondary" href="https://github.com/theBowja/genshin-db" target="_blank" rel="noreferrer">Inspect GenshinDB</a></section></div>;
}
